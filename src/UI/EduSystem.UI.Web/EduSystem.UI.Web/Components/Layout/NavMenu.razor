@inject NavigationManager NavigationManager

<MudNavMenu>
    <MudNavLink Href="/" Icon="@Icons.Material.Filled.Dashboard" Match="NavLinkMatch.All">
        Dashboard
    </MudNavLink>
    <MudNavGroup Title="Defianation" Icon="@Icons.Material.Filled.MenuBook" Expanded="@IsDefinitionExpanded()">
        
        <MudNavLink Href="/teachers" Icon="@Icons.Material.Filled.Class">
            Teachers
        </MudNavLink>
        <MudNavLink Href="/students" Icon="@Icons.Material.Filled.Person">
            Students
        </MudNavLink>
    </MudNavGroup>

    <MudNavLink Href="/classes" Icon="@Icons.Material.Filled.Class">
        Classes
    </MudNavLink>

    <MudNavLink Href="/attendance" Icon="@Icons.Material.Filled.CheckCircle">
        Attendance
    </MudNavLink>

    <MudNavLink Href="/exams" Icon="@Icons.Material.Filled.Assignment">
        Exams
    </MudNavLink>

    <MudNavLink Href="/results" Icon="@Icons.Material.Filled.AssignmentTurnedIn">
        Results
    </MudNavLink>

    <MudDivider Class="my-2" />

    <MudNavLink Href="/settings" Icon="@Icons.Material.Filled.Settings">
        Settings
    </MudNavLink>
</MudNavMenu>

@code {
    private bool IsDefinitionExpanded()
    {
        var currentUrl = NavigationManager.Uri.ToLower();
        return currentUrl.Contains("/teachers") || currentUrl.Contains("/students");
    }
}